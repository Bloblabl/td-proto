{
  "name": "td-proto",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "smoke": "tsx smoke.ts",
    "deploy": "npm run build && cd dist && git init -b gh-pages && git add -A && git commit -m deploy && git push -f https://github.com/Bloblabl/td-proto.git gh-pages && cd .. && node -e \"require('fs').rmSync('dist/.git',{recursive:true,force:true})\""
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "^5.5.4",
    "vite": "^6.3.5"
  }
}
